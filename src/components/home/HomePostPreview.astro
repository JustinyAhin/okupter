---
const { slug, title, description, date, readingTime } = Astro.props;
---

<article class="p-8 space-y-2 bg-white rounded-sm shadow-sm">
  <h3>
    <a href={`archives/${slug}`} class="no-underline">{title}</a>
  </h3>
  <p>{description}</p>
  <div class="flex text-sm">
    <p>{date}&nbsp</p>
    <span>-</span>
    <p>&nbsp{readingTime} min</p>
  </div>
</article>

<style>
  a {
    @apply no-underline;
  }
</style>

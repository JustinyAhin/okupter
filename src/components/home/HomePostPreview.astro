---
const { slug, title, description, date } = Astro.props;
---

<article class="p-8 space-y-2 bg-white rounded-sm shadow-sm">
  <h3>
    <a href={`archives/${slug}`} class="no-underline">{title}</a>
  </h3>
  <p>{description}</p>
  <div class="flex text-sm">
    <p>{date}</p>
  </div>
</article>

<style>
  a {
    @apply no-underline;
  }
</style>
